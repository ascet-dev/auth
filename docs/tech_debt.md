# Tech debt

## Безопасность (не блокеры, но чинить стоит)

### ⛔ Сделать ДО того, как в проде появится первый OAUTH-коннектор

OAuth-флоу сейчас недостижим: коннекторы типа OAUTH не сидятся, а `resolve_auth_connector`
без них отдаёт «not configured». Но включение — это полминуты в админке без единой строки
кода, после чего пункты ниже становятся живыми дырками.

- [ ] **OAuth: нет CSRF-защиты authorization code flow.** `state` генерируется в
      `start_oauth_flow`, но нигде не хранится, а `login_by_oauth` его не принимает.
      Нужно: хранить state (короткоживущая запись/кеш) + сверять при обмене кода.
- [ ] **id_token не верифицируется** (`jwt_decode(..., verify_signature=False)`) —
      подпись провайдера не проверяется, хотя `jwks_url` уже есть в settings коннектора.
- [ ] **Полный `token_response` провайдера пишется в `credential.meta`** открытым текстом
      (включая access/refresh токены провайдера) — чинить попутно, это несколько строк.
- [ ] **Нет rate-limit по IP** на login-эндпоинтах. Lockout теперь атомарный и per-credential,
      но распределённый перебор по многим логинам ничем не ограничен. Делать на уровне
      ingress/middleware, не в сервисе. Тем же закрывается DoS «держать учётку залоченной,
      тратя max_failed_attempts попыток за окно».
- [ ] `allow_registration=false` даёт лишь частичную изоляцию: credential-ы глобальны,
      а `client_app_id` в `/auth/register/password` необязателен — зарегистрировавшись
      через другое приложение, юзер сможет войти туда, где регистрация закрыта.
- [ ] refresh-токен админки в localStorage (XSS-поверхность). Hardening — httpOnly-cookie
      + CSRF-защита, потребует новой backend-поверхности.
- [ ] коннекторы одного типа не изолированы друг от друга: TMA/OAuth-credential ищется
      по `external_subject_id` без учёта коннектора, поэтому владелец любого бота может
      войти как пользователь другого бота того же инстанса. Это оборотная сторона
      «одна identity через любых ботов» (яндекс-модель) — намеренно, но если появятся
      несвязанные продукты на одном инстансе, изоляцию включать по `credential.provider`.
      Пока смягчено тем, что заводить коннекторы может только OWNER.
- [ ] Ошибки REQUEST-компонентов (`CurrentIdentity`) в пользовательских эндпоинтах
      маппятся в 500; в админских — в 401 через `admin_scope`. Унифицировать.

## Функциональность

- [ ] `client_app.access_token_ttl_sec` не используется: access TTL всегда глобальный
      `cfg.auth.access_token_lifetime` (1 мин). Брать из client_app в `build_jwt_payload`.
- [ ] SSO между приложениями отсутствует: сессии и токены per-app, центральной
      login-страницы с authorization code flow нет.
- [ ] Мультивкладочный refresh: токен single-use, дедупликация только внутри вкладки
      (`frontend/src/api/client.ts`). Две вкладки с истёкшим access-токеном могут
      разлогинить друг друга (частично закрыто: чужой свежий токен не стирается).
- [ ] политику lockout выбирает вызывающий: счётчики глобальны на credential, а порог
      берётся из парольного коннектора приложения, поэтому вход через приложение
      со слабым порогом «разряжает» счётчик перед входом в строгое (родственно обходу
      `allow_registration`). Чинится привязкой политики к credential, а не к приложению.
- [ ] `settings/doc.py` (`cfg.doc`) не подключён к `Web.doc` — Swagger с дефолтным тайтлом.
- [ ] `ENV` читается и из процесса (`settings/env.py`), и через `cfg.env` — два источника
      истины, `.env` может их рассинхронизировать.

## Инфраструктура

- [ ] `make keys` кладёт приватный ключ с правами 644, потому что в контейнере процесс
      идёт под своим uid: на нативном Linux он не совпадает с хостовым, и mount с 600
      был бы нечитаем. Для прода ключи выдаёт секрет-стор платформы (Docker/K8s secrets)
      с нормальным владельцем — этот таргет только для локального стека.

## Закрыто

- [x] auth-схема не создавалась перед миграциями — `alembic/env.py` делает
      `CREATE SCHEMA IF NOT EXISTS` до старта миграций
- [x] Системный `auth-admin` client_app можно было заархивировать/переконфигурировать
      через админку (полный отказ входа в админку) — теперь неизменяем через API,
      `key` уникален (миграция 0002)
- [x] Lockout обходился параллельными запросами (read-modify-write счётчика) —
      инкремент считает БД; счётчик обнуляется по истечении окна блокировки
- [x] Дефолтные RSA-ключи из репозитория позволяли подписать админский токен —
      ключей по умолчанию больше нет вообще, без них сервис не стартует,
      `make keys` генерирует пару в `secrets/`
- [x] `bootstrap-owner` мог отдать OWNER существующей (чужой) учётке и игнорировал
      переданный пароль — теперь отказ, `--adopt-existing` со сбросом пароля
- [x] `identity.status` не проверялся при выдаче токенов — проверка в `build_jwt_payload`
      (единая точка), refresh ревокает сессию заблокированной identity
- [x] Архивированный client_app продолжал выдавать и обновлять сессии
- [x] `settings` коннекторов не типизировались (строка вместо числа → 500 в рантайме)
- [x] Явный `null` в PATCH доезжал до БД и падал на NOT NULL с 500 — теперь
      `exclude_none` в generic-базе: null = «не менять»
- [x] Состояние блокировки было наблюдаемо снаружи (400 `Credential is locked` против
      401 на несуществующем логине) — ответ унифицирован в 401 `Invalid credentials`
- [x] Гонка на unique-индексе давала 500 — `conflict_on_unique` маппит
      `UniqueViolationError` в 409
- [x] Эскалация ADMIN → OWNER: публичные login-эндпоинты принимали системный
      `client_app_id` (а по нему в токен кладётся `role`), плюс ADMIN мог завести
      TMA-коннектор со своим ботом и подделать субъекта. Закрыто: системное приложение
      запрещено в публичных флоу, коннекторы и их привязка — только OWNER
- [x] partial unique индексы существовали только в миграции — следующий autogenerate
      снёс бы их; объявлены в моделях через `__table_args__`
- [x] compose публиковал порты на все интерфейсы при дефолтных кредах владельца —
      теперь только `127.0.0.1` (переопределяется `BIND_HOST`)
- [x] `make test` мог снести дев-базу, если `PG__CONNECTION__DSN` экспортирован в шелле —
      conftest требует имя БД на `_test`
- [x] беспрефиксные `PRIVATE_KEY`/`PUBLIC_KEY` подхватывались как ключи — `require_keys`
      проверяет, что это PEM
- [x] ADMIN мог ревокать сессии OWNER-а; ошибки грантов «not found» отдавались как 409;
      неаутентифицированному сообщали про непроинициализированный сервис
- [x] админка залипала на спиннере при ошибке запроса (DataTable не отличал ошибку
      от загрузки) и не уводила на /login при окончательной потере сессии
- [x] Отсутствие PEM по `AUTH__*_KEY_PATH` падало сырым `FileNotFoundError` —
      теперь понятное сообщение с подсказкой `make keys`
